<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.phpopenredirectcompanion</id>
    <name>PHP Open Redirect Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on a <code>header('Location: ...')</code> call
        whose argument is built directly from a PHP superglobal
        (<code>$_GET</code>, <code>$_POST</code>,
        <code>$_REQUEST</code>, <code>$_COOKIE</code>). This is the
        textbook Open Redirect anti-pattern (CWE-601): an attacker
        crafts a link to the trusted site that redirects the victim to
        an attacker-controlled domain, commonly used for phishing
        since the initial URL still shows the trusted host. OWASP's
        own "Unvalidated Redirects and Forwards" guidance documents
        this as a real, well-known vulnerability class.</p>
        <p>Confirmed real gap: "PHP Inspections (EA Extended)" (one of
        the most widely used PHP inspection plugins on Marketplace)
        does not cover open redirect detection anywhere in its
        documented security feature list -- confirmed by reading it
        before building this.</p>
        <p><b>100% static text analysis</b> -- a regex-based line
        scanner, not a real PHP parser, so it works whether the PHP
        plugin is installed or not.</p>
        <p>Stated honestly: v0.1 only flags a <code>header(</code> call
        whose argument text contains both <code>Location:</code> and a
        direct superglobal reference on the same line. A redirect
        built from an intermediate variable assigned from a superglobal
        several lines earlier isn't traced (real data-flow analysis,
        out of scope for a text scanner).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="PHP Open Redirect Companion"
            displayName="Location redirect built from a superglobal"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.phpopenredirectcompanion.inspection.OpenRedirectInspection"/>
        <notificationGroup id="PHP Open Redirect Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
